name: second workflow
on: workflow_dispatch
jobs:
  second-job:
    runs-on: ubuntu-latest
    steps:
        - name : akash kumar
          run : echo "Arad"
        - name : Balaji
          run : echo "Balaji"
      
